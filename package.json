{
  "name": "nexora-institucional",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio institucional estático desarrollado con HTML, SASS y Bootstrap.",
  "scripts": {
    "sass": "sass sass/main.scss assets/css/main.css --style=expanded",
    "watch": "sass --watch sass/main.scss:assets/css/main.css"
  },
  "devDependencies": {
    "sass": "^1.90.0"
  }
}
